새롭게 추가된 **`7eerup/portfolio`** 저장소까지 포함하여 3개 저장소를 비교해 드립니다.

---

### **1. `7eerup/portfolio` 저장소 특징**
* **목적/성격:** 순수 HTML, CSS, JavaScript만 사용하여 제작한 반응형 포트폴리오 웹사이트입니다.
* **폴더 구조:** 과제 기본 표준 구조인 `css/`, `images/`, `js/`, `index.html`, `README.md`로 매우 깔끔하게 구성되어 있습니다.
* **언어 구성 비율:** **JavaScript 54.6%**, **CSS 33%**, **HTML 12.4%**.
* **주요 기능:** 필수 요구사항(반응형 레이아웃, 다크 모드, 햄버거 메뉴, 스크롤 애니메이션, GitHub API 연동 및 상태 처리, 폼 유효성 검사) 외에도 보너스 과제 항목인 **프로젝트 언어별 필터링 기능**까지 완성되어 있습니다.

---

### **2. 3개 저장소 핵심 비교**

| 구분 | `CMS-SUDO7/CODY-B4-1` | `7eerup/portfolio` (신규) | `dolphin1404/dolphin1404.github.io` |
| --- | --- | --- | --- |
| **구조 복잡도** | **기본 표준 구조** (`css/`, `js/`, `images/`) | **기본 표준 구조** (`css/`, `js/`, `images/`) | **다국어/확장 구조** (`en/`, `ko/`, `admin/` 등) |
| **커밋 수** | 12개 커밋 | 4개 커밋 | 18개 커밋 |
| **주요 학습 포인트** | README의 상세한 **상태(STATE) 관리 흐름** 및 레이아웃 선택 기준 설명 | **핵심 요구사항 + 프로젝트 필터링(보너스 기능)** | 기존 블로그 사이트 확장 및 다국어 지원 |

---

### **3. 초보자를 위한 학습 가이드**

* **전체적인 코드 구성과 흐름을 따라 할 때 → `7eerup` 추천**
  * 커밋 수가 4개로 간결하고, 과제에 딱 필요한 로직 위주로 정리되어 있어 전체 코드를 한눈에 파악하기 가장 좋습니다. 보너스 과제인 **프로젝트 필터링** 구현 방식도 참고할 수 있습니다.
* **상태 관리 원리와 설명 방식을 참고할 때 → `CMS-SUDO7` 추천**
  * README에 `STATE` 객체 기반의 **이벤트 → 상태 변경 → UI 렌더링** 흐름과 Flexbox/Grid 활용 이유가 가장 정석적으로 문서화되어 있습니다.

**💡 추천 활용법:** **`7eerup` 님의 코드**를 보면서 구조를 따라 작성해 보시고, **`CMS-SUDO7` 님의 README**를 참고하여 상태 관리 개념과 평가 질문 답변을 준비하시는 조합을 권장합니다!

---

🔍 `7eerup` 님이 구현한 프로젝트 필터링 로직이나 GitHub API 연동 코드 방식을 자세히 살펴볼까요?
권창범 [https://github.com/7eerup/portfolio]
채민성 [https://github.com/CMS-SUDO7/CODY-B4-1]
윤대영 [https://github.com/myy-dev/codessey/tree/main/B4-1]
**`myy-dev/codessey` (B4-1)** 저장소는 **완성도와 코드 구조 면에서 완성형에 가까운 모범 답안** 역할을 해주는 프로젝트입니다.

---

### **1. `myy-dev/codessey/B4-1` 의 주요 특징**

* **완벽한 요구사항 + 모든 보너스 과제 달성**
  * 기본 미션 요구사항은 물론, 보너스 과제 4가지(**언어별 프로젝트 필터링, Hero 섹션 타이핑 효과, Formspree/EmailJS 실제 이메일 전송, 시스템 다크 모드 자동 감지**)를 모두 완성한 상태입니다.
* **세분화된 CSS 모듈화 구조**
  * `css/` 폴더 안에 스타일을 역할별로 세분화하여 작성했습니다.
    * `base.css` (기본 변수), `layout.css` (헤더/푸터), `components.css` (버튼/카드), `sections.css` (섹션별 스타일), `responsive.css` (반응형) 등으로 나누고 `style.css`에서 하나로 합치는 체계적인 구조입니다.
* **배포 주소**: [https://myy-dev.github.io/B4-1/](https://myy-dev.github.io/B4-1/)

---

### **2. 초보자 관점에서의 장단점**

* **장점**
  * **보너스 기능 참고용 최고의 가이드**: 타이핑 효과나 실제 이메일 전송, 시스템 테마 감지 등 고급 기능을 어떻게 구현하는지 코드를 직접 확인할 수 있습니다.
  * **실무형 코드 구조**: CSS를 부품처럼 나누어 관리하는 방식을 배울 수 있어 실무적인 코드 작성 습관을 익히기에 좋습니다.
* **단점 (초보자 기준)**
  * 프론트엔드를 처음 접하는 단계에서는 CSS 파일이 7개로 나뉘어 있으면 원하는 스타일을 찾거나 수정할 때 파일 간 이동이 다소 헷갈릴 수 있습니다.

---

### **💡 최종 학습 전략 추천**

1. **기초 뼈대 잡기**: 이전 답변에서 다룬 `7eerup` 또는 `CMS-SUDO7` 님처럼 직관적인 구조에서 HTML/CSS/JS 기본 흐름을 먼저 익힙니다.
2. **기능 확장 및 고도화**: **`myy-dev` 님의 저장소**에서 타이핑 애니메이션이나 이메일 전송, 세분화된 CSS 모듈화 방식을 참고하여 본인의 프로젝트에 하나씩 적용해보시는 방식을 추천합니다!

`myy-dev` 님이 구현한 **타이핑 효과**나 **실제 이메일 전송(Formspree/EmailJS)** 코드 로직을 한번 같이 분석해 볼까요?
이규민 [https://github.com/dolphin1404/dolphin1404.github.io]
